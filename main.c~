#include <stdio.h>
#include "block.h"

int main(void){
  GROUP group;
  int debug_loop;
  SetMino();
  for(debug_loop = 0; debug_loop < MAX_MINO;debug_loop++){
    PrintMino(group.block[debug_loop].shape);
  }
  return 0;
}
